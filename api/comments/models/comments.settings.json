{
  "kind": "collectionType",
  "collectionName": "comments",
  "info": {
    "name": "Comments"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "body": {
      "type": "richtext",
      "required": true
    },
    "user": {
      "plugin": "users-permissions",
      "model": "user",
      "via": "comments"
    },
    "article": {
      "model": "articles",
      "via": "comments"
    }
  }
}
